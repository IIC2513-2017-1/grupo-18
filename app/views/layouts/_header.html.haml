%body
  %nav
    - if logged_in?
      %strong.nav Logged as #{user_name}
      %i.nav (#{user_type_string})
    - else

    %li.nav= link_to "Home",   root_path
    %li.nav= link_to "Users",   users_path
    - if logged_in?
      %li.navR= link_to "Log out", logout_path, method: :delete
      %li.navR= link_to "Add friend", '#'
      %li.navR= link_to "New bet", new_bet_path
    - else
%br/