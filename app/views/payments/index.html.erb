<html>
  <head>
    <!-- Nothing here yet -->
  </head>
  <body>
    <% content_for :content do %>
      <div class="UI_column">
        <h1>Payments by user</h1>

        <% allUsers.each do |user| %>
          <div>
            <h2><%="To #{user.name || user.username}:"%></h4>
            <ul>
              <% user.payments.each do |payment| %>
              <li class="UI_form_column">
                <p> <strong>Amount:</strong><%= " #{payment.amount}" %> </p>
                <p><strong>Date:</strong> <%= " #{payment.created_at}"%></p>
              </li>
              <% end %>
            </ul>
            <br>
          </div>
        <% end %>
      </div>
    <% end %>
  </body>
</html>