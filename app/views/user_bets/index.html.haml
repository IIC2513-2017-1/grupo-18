%p#notice= notice
%h1 User Bets
%table
  %thead
    %tr
      %th Amount
      %th User
      %th Bet
      %th{:colspan => "3"}
  %tbody
    - @user_bets.each do |user_bet|
      %tr
        %td= user_bet.amount
        %td= user_bet.user
        %td= user_bet.bet
        %td= link_to 'Show', user_bet
        %td= link_to 'Edit', edit_user_bet_path(user_bet)
        %td= link_to 'Destroy', user_bet, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New User Bet', new_user_bet_path
